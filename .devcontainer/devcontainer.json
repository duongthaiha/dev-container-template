{
  "name": "Python Azure Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-azurecli",
        "ms-azuretools.azd",
        "eamodio.gitlens"
      ]
    }
  },
  "postCreateCommand": "python --version && az --version && azd version && bicep --version && oh-my-posh version && mkdir -p .agents/skills && (cp -rn /opt/microsoft-skills/.agents/skills/. .agents/skills/ 2>/dev/null && echo 'Skills synced from baked image cache' || echo 'No baked skills found at /opt/microsoft-skills')",
  "remoteUser": "vscode"
}
